package edu.neumont.csc250;

import java.util.List;

public class SelectionSorter<T extends Comparable<T>> implements Sorter<T>{

	@SuppressWarnings("unchecked")
	@Override
	public void sort(List<T> unsorted) {

		T[] array = (T[]) new Object[unsorted.size()];

		for (int i = 0; i < unsorted.size(); i++){
			array[i] = unsorted.get(i);
		}
		
		for (int i = 1; i < unsorted.size(); i++){
			T min = null;

			for (int j = i; j < unsorted.size(); j++){
				if (min == null){
					min = array[j];
				}
				else if (min.compareTo(unsorted.get(j)) > 0 ){
					min = array[j];
				}
			}
		}

		unsorted.clear();
	}

}
